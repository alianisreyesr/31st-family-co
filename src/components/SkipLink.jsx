/** Primer elemento tabulable de la página: salta la navegación repetida. */
export function SkipLink() {
  return (
    <a className="skip-link" href="#contenido">
      Saltar al contenido
    </a>
  )
}
